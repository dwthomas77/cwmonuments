import React from 'react';
import { Block } from 'components/Base';

const InteractiveMap = (props) => {
    return (
        <Block {...props}>InteractiveMap</Block>
    );
};

export default InteractiveMap;
